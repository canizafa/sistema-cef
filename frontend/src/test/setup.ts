// Configuración que se ejecuta antes de cada prueba automática (test suite).
// Activa funciones de verificación extra (matchers) como "este botón está visible" o "este campo tiene este valor".
// Se enlaza con Vitest a través del campo setupFiles en vite.config.ts.
